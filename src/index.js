import ReactDOM from 'react-dom';
import React, { StrictMode } from 'react';
import MCQ from './MCQ'

ReactDOM.render(
  <StrictMode>
    <MCQ />
  </StrictMode>,
  document.getElementById('root')
);

